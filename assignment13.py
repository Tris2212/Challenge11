diameter = float(input("Input the diameter of the circle: "))
radius = diameter / 2
multiply = radius * radius
pi = 3.14
outcome = multiply * pi

print("The surface of the circle is " + str(outcome) + " m2")